---
'@uniqs/utils': minor
---

Add some new util functions:

- `randomRange()`
- `stableSort()`
- `getMidnight()`
- `convertToDateString()`
- `compareTitle()`
- `titleConverter()`
- `isXXX()`

Write some tests for utils
